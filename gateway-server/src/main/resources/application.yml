server:
  port: 8072

spring:
  application:
    name: gateway-server
  cloud:
    gateway:
    discovery:
      locator:
      enabled: true


management:
  endpoints:
    web:
      exposure:
        include: "gateway,health,info"

  endpoint:
    gateway:
      access: read_only